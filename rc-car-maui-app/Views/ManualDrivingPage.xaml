<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="rc_car_maui_app.Views.ManualDrivingPage">
    <ContentPage.Content>
        <Grid>
            <!-- Your main content (e.g., map, camera feed) -->
            <!-- <YourMainContentView /> -->

            <!-- Overlay panel -->
            <Grid HorizontalOptions="End" VerticalOptions="End" BackgroundColor="#88000000">
                <Slider Rotation="-90"
                        ValueChanged="Slider_OnValueChanged"
                        DragCompleted="Slider_OnDragCompleted"
                        Minimum="-100"
                        Maximum="100"
                        Value="0"
                        WidthRequest="150"
                        HeightRequest="150" />
            </Grid>
        </Grid>
    </ContentPage.Content>
</ContentPage>